# Markdown 编辑器功能演示

欢迎使用这个现代化的 Markdown 编辑器！本文档展示了所有支持的功能。

## 文本格式化

### 基础格式

这是 **加粗文本**，这是 *斜体文本*，这是 ~~删除线文本~~。

你也可以组合使用：***加粗斜体***，**_加粗斜体_**。

### 行内代码

使用 `console.log()` 来输出调试信息。在 JavaScript 中，`const` 和 `let` 用于声明变量。

## 标题层级

# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题

## 列表

### 无序列表

- 第一项
- 第二项
  - 嵌套项 2.1
  - 嵌套项 2.2
- 第三项

### 有序列表

1. 第一步
2. 第二步
   1. 子步骤 2.1
   2. 子步骤 2.2
3. 第三步

### 任务列表

- [x] 完成项目初始化
- [x] 实现核心编辑器
- [x] 添加工具栏
- [ ] 添加主题切换
- [ ] 实现文件系统集成

## 引用

> 这是一个引用块。
> 
> 引用可以包含多个段落。
> 
> > 这是嵌套引用。

## 代码块

### JavaScript 示例

```javascript
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10)); // 输出: 55
```

### Python 示例

```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + middle + quick_sort(right)

print(quick_sort([3, 6, 8, 10, 1, 2, 1]))
```

### TypeScript 示例

```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

class UserService {
  private users: User[] = [];

  addUser(user: User): void {
    this.users.push(user);
  }

  getUserById(id: number): User | undefined {
    return this.users.find(u => u.id === id);
  }
}
```

## 表格

| 功能 | 状态 | 优先级 | 备注 |
|------|------|--------|------|
| Markdown 解析 | ✅ 完成 | 高 | 使用 TipTap |
| 代码高亮 | ✅ 完成 | 高 | 支持 30+ 语言 |
| 数学公式 | ✅ 完成 | 中 | 使用 KaTeX |
| 主题切换 | ⏳ 进行中 | 中 | 计划支持 |
| PDF 导出 | 📋 计划中 | 低 | 未来版本 |

## 数学公式

### 行内公式

爱因斯坦的质能方程：$E = mc^2$

圆的面积公式：$A = \pi r^2$

### 块级公式

欧拉公式：

$$
e^{i\pi} + 1 = 0
$$

二次方程求根公式：

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

积分示例：

$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

矩阵示例：

$$
\begin{bmatrix}
a & b \\
c & d
\end{bmatrix}
\begin{bmatrix}
x \\
y
\end{bmatrix}
=
\begin{bmatrix}
ax + by \\
cx + dy
\end{bmatrix}
$$

## 水平分隔线

上面的内容

---

下面的内容

## 链接

[访问 GitHub](https://github.com)

[TipTap 官方文档](https://tiptap.dev)

## 组合示例

### 代码 + 数学

计算斐波那契数列的时间复杂度是 $O(2^n)$，但使用动态规划可以优化到 $O(n)$：

```python
def fib_dp(n):
    if n <= 1:
        return n
    dp = [0] * (n + 1)
    dp[1] = 1
    for i in range(2, n + 1):
        dp[i] = dp[i-1] + dp[i-2]
    return dp[n]
```

时间复杂度：$T(n) = O(n)$，空间复杂度：$S(n) = O(n)$

---

**提示**: 使用工具栏可以快速插入各种 Markdown 元素！

